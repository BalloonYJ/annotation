#annotation tools

This code could be used to annotate grount truth of object detections.

To use this tool:

Run the file main.m
select the imgage directory and input sequence length.
If current image need to be processed, click "yes" in the dialog box,draw and drag rectangles in each image,double click to add the rectangle to result.If current image has been finished, click "no" to the next image.


