#annotation tools

This code could be used to annotate grount truth of object detections.

To use this tool:

1.Run the file main.m
2.Select the imgage directory and input sequence length.Images in selected directory should be name in format: ("%06d.jpg",frame)
3.If current image need to be processed, click "yes" in the dialog box,draw and drag rectangles in each image,double click to add the rectangle to result.If current image has been finished, click "no" to the next image.


